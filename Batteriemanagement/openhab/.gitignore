# Transiente Build-Artefakte von setup/build-dist.sh (werden nach dem
# Paketbau geloescht, bleiben aber liegen, wenn der Build abbricht)
BUILD-INFO
inverters/*/page-*.json

# Replay-Daten (Betriebsdaten der Anlagen aus der Datenbank, nie einchecken)
control/replay/*.csv
control/replay/*.txt
control/replay/core_old.js
